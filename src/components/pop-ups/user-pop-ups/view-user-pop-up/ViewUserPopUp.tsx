import { User } from "../../../../models/user/User"

interface Props {
  user: User
}

export const ViewUserPopUp = ({user}: Props) => {
  <div className="view-user-pop-up-container pop-up-component-container"></div>
}